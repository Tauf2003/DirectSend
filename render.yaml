services:
  - type: web
    name: directsend
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build:client
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
